from pathlib import Path

try:
    base_path = Path(__file__).resolve().parent.parent  # if running from a script
except NameError:
    base_path = Path.cwd()  # fallback if in notebook or interactive

schema_path = base_path / "schema_folder" / "test.avsc"

with schema_path.open("r", encoding="utf-8") as f:
    schema = f.read()
