# Install minimal dependencies (Python 3, Chromium, ChromeDriver, and necessary libraries)
RUN dnf -y update && \
    dnf -y install \
    python3 \
    python3-pip \
    chromium \
    chromium-chromedriver \
    X11-server-utils \
    dbus-x11 \
    alsa-lib && \
    dnf clean all
