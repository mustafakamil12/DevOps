openssl pkcs12 -in my_cert.pfx -nocerts -nodes -out client-key.pem

openssl pkcs12 -in my_cert.pfx -clcerts -nokeys -out client-cert.pem


openssl pkcs12 -in my_cert.pfx -cacerts -nokeys -out ca-cert.pem


conf = {
    "bootstrap.servers": "your_kafka_broker:9093",
    "security.protocol": "SSL",
    "ssl.key.location": "client-key.pem",
    "ssl.certificate.location": "client-cert.pem",
    "ssl.ca.location": "ca-cert.pem"
}
